[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    slow: long-running tests
    skipunless: skip test when condition not met (usage: @pytest.mark.skipunless("CONDITION", ...))
