---
phase: 4
plan: 04
wave: 4
objective: Implement custom reporting and dashboard performance optimization
files_modified:
  - src/api/reports.py
  - src/dashboard/main.py
  - tests/test_phase4_reports.py
autonomous: true
---

# Plan 4-04: Advanced Reporting & Performance

## Objective
Create custom report generation framework (PDF/CSV) and optimize dashboard performance to < 3s load time with caching.

## Tasks

### T4.1: Create report generation API
<read_first>
- src/api/main.py (existing FastAPI patterns)
- src/api/schemas.py (existing Pydantic patterns)
</read_first>

<action>
Create `src/api/reports.py` with:
- `POST /api/v1/reports` endpoint
- Accepts filters: industry, salary_min, salary_max, skills, city
- Returns CSV or PDF based on format parameter
- Uses pandas for data aggregation
- Streaming response for large reports
</action>

<acceptance_criteria>
- File `src/api/reports.py` exists
- POST endpoint accepts filter parameters
- Endpoint returns CSV format
- Endpoint returns PDF format
- Streaming works for large datasets
</acceptance_criteria>

### T4.2: Optimize dashboard performance
<read_first>
- src/dashboard/main.py (existing Streamlit patterns)
</read_first>

<action>
Update `src/dashboard/main.py` with:
- Add `@st.cache_data` decorator to data loading functions
- Implement lazy loading for charts
- Add query caching with 5-minute TTL
- Optimize SQL queries with proper indexes
</action>

<acceptance_criteria>
- Dashboard load time < 3s (measured manually)
- Cache decorators applied to data functions
- Lazy loading implemented for charts
- No regression in existing functionality
</acceptance_criteria>

### T4.3: Create report tests
<read_first>
- tests/test_phase2.py (test patterns)
- src/api/reports.py (report API)
</read_first>

<action>
Create `tests/test_phase4_reports.py` with:
- `test_report_csv_generation()` - returns CSV data
- `test_report_pdf_generation()` - returns PDF data
- `test_report_with_filters()` - filters work correctly
- `test_dashboard_performance()` - loads in < 3s
</action>

<acceptance_criteria>
- File `tests/test_phase4_reports.py` exists
- All tests pass with `python -m pytest tests/test_phase4_reports.py -v`
</acceptance_criteria>

## must_haves
- [ ] Custom report API working (CSV/PDF)
- [ ] Dashboard load time < 3s
- [ ] Query caching implemented
- [ ] Lazy loading for charts
