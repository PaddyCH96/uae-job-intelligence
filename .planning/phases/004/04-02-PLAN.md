---
phase: 4
plan: 02
wave: 2
objective: Implement user profile system with opt-in and skill gap analysis
files_modified:
  - src/models/user_profile.py
  - src/api/profiles.py
  - migrations/007_phase4_profiles.sql
  - tests/test_phase4_profiles.py
autonomous: true
---

# Plan 4-02: User Profile System (Opt-In)

## Objective
Design and implement an opt-in user profile system with personalized skill gap analysis. Users can save preferences and see which skills they're missing compared to market demand.

## Tasks

### T2.1: Create user profile schema
<read_first>
- migrations/001_init_schema.sql (existing schema patterns)
- src/database/models.py (existing models)
</read_first>

<action>
Create `migrations/007_phase4_profiles.sql` with:
- `analytics.dim_user_profile` table (user_id UUID PK, opt_in BOOLEAN, created_at TIMESTAMP, skills_interests TEXT[], salary_expectation_min NUMERIC, salary_expectation_max NUMERIC)
- All columns nullable except user_id and opt_in
- Index on opt_in for fast filtering
</action>

<acceptance_criteria>
- File `migrations/007_phase4_profiles.sql` exists
- Table `dim_user_profile` is created
- Table has opt_in column with default False
</acceptance_criteria>

### T2.2: Create profile API endpoints
<read_first>
- src/api/main.py (existing FastAPI patterns)
- src/api/schemas.py (existing Pydantic patterns)
</read_first>

<action>
Create `src/api/profiles.py` with:
- `POST /api/v1/profiles` - create/update profile (opt-in required)
- `GET /api/v1/profiles/{user_id}` - get profile
- `DELETE /api/v1/profiles/{user_id}` - delete profile (anonymize data)
- All endpoints check opt_in flag before returning data
- Use existing database session patterns
</action>

<acceptance_criteria>
- File `src/api/profiles.py` exists
- POST endpoint creates profile only with opt_in=True
- GET endpoint returns profile data
- DELETE endpoint removes profile data
- All endpoints have proper error handling
</acceptance_criteria>

### T2.3: Implement skill gap analysis
<read_first>
- src/models/salary_predictor.py (model patterns)
- src/api/profiles.py (profile data)
</read_first>

<action>
Create `src/models/user_profile.py` with:
- `analyze_skill_gap(user_id)` function
- Compares user's skills_interests with top market skills from v_skill_growth_rates
- Returns: missing_skills, skill_match_percentage, recommended_skills
- Cache results for 24 hours
</action>

<acceptance_criteria>
- File `src/models/user_profile.py` exists
- Function `analyze_skill_gap()` returns missing_skills list
- Function returns skill_match_percentage
- Function returns recommended_skills list
</acceptance_criteria>

### T2.4: Create profile tests
<read_first>
- tests/test_phase2.py (test patterns)
- src/api/profiles.py (API endpoints)
</read_first>

<action>
Create `tests/test_phase4_profiles.py` with:
- `test_profile_creation_with_optin()` - POST creates profile
- `test_profile_creation_without_optin()` - POST rejects without opt_in
- `test_profile_retrieval()` - GET returns profile
- `test_profile_deletion()` - DELETE removes profile
- `test_skill_gap_analysis()` - returns missing skills
</action>

<acceptance_criteria>
- File `tests/test_phase4_profiles.py` exists
- All tests pass with `python -m pytest tests/test_phase4_profiles.py -v`
</acceptance_criteria>

## must_haves
- [ ] User profile schema created
- [ ] Profile CRUD API working
- [ ] Opt-in required for data storage
- [ ] Skill gap analysis returns results
- [ ] Privacy/opt-out mechanism working
