---
phase: 4
plan: 01
wave: 1
objective: Train predictive models for skill demand forecasting and salary prediction
files_modified:
  - src/models/skill_forecast.py
  - src/models/salary_predictor.py
  - migrations/006_phase4_models.sql
  - tests/test_phase4_models.py
autonomous: true
---

# Plan 4-01: Predictive Models & Forecasting

## Objective
Train scikit-learn models for skill demand forecasting (3-month horizon) and salary prediction. Create SQL views for forecasts and compute evaluation metrics (R², MAE, RMSE).

## Tasks

### T1.1: Create skill demand forecasting model
<read_first>
- src/utils/llm.py (existing LLM patterns)
- src/ingestion/processor.py (data processing patterns)
- migrations/005_phase3_views.sql (existing views)
</read_first>

<action>
Create `src/models/skill_forecast.py` with:
- `train_skill_demand_model()` function using RandomForestRegressor
- Features: skill_count, tech_count, experience_level, employment_type
- Target: job_count (aggregated by skill per month)
- Split data 80/20 train/test
- Save model to `models/skill_forecast_v1.pkl`
</action>

<acceptance_criteria>
- File `src/models/skill_forecast.py` exists
- Function `train_skill_demand_model()` is defined
- Model is trained and saved to `models/skill_forecast_v1.pkl`
- R² score printed to stdout
</acceptance_criteria>

### T1.2: Create salary prediction model
<read_first>
- src/models/skill_forecast.py (patterns from T1.1)
- analytics.v_salary_correlation (existing salary data)
</read_first>

<action>
Create `src/models/salary_predictor.py` with:
- `train_salary_model()` function using Ridge regression
- Features: skill_count, tech_count, experience_level_id
- Target: salary_midpoint
- Split data 80/20 train/test
- Save model to `models/salary_predictor_v1.pkl`
</action>

<acceptance_criteria>
- File `src/models/salary_predictor.py` exists
- Function `train_salary_model()` is defined
- Model is trained and saved to `models/salary_predictor_v1.pkl`
- MAE and R² scores printed to stdout
</acceptance_criteria>

### T1.3: Create forecast SQL views
<read_first>
- migrations/005_phase3_views.sql (existing view patterns)
- src/models/skill_forecast.py (model output format)
</read_first>

<action>
Create `migrations/006_phase4_models.sql` with:
- `v_skill_forecast` view: skill_name, predicted_demand, confidence_interval
- `v_salary_prediction` view: skill_count, tech_count, predicted_salary, confidence_interval
- Add sentiment_score and industry_id columns to fact_job_posting (nullable)
- Create dim_industry table
</action>

<acceptance_criteria>
- File `migrations/006_phase4_models.sql` exists
- View `v_skill_forecast` is queryable
- View `v_salary_prediction` is queryable
- Columns `sentiment_score` and `industry_id` exist in fact_job_posting
</acceptance_criteria>

### T1.4: Create model evaluation tests
<read_first>
- tests/test_phase2.py (test patterns)
- tests/test_phase3.py (test patterns)
</read_first>

<action>
Create `tests/test_phase4_models.py` with:
- `test_skill_forecast_model_trained()` - checks model file exists
- `test_salary_predictor_model_trained()` - checks model file exists
- `test_skill_forecast_view_exists()` - queries v_skill_forecast
- `test_salary_prediction_view_exists()` - queries v_salary_prediction
- `test_model_evaluation_metrics()` - checks R² > 0.3
</action>

<acceptance_criteria>
- File `tests/test_phase4_models.py` exists
- All tests pass with `python -m pytest tests/test_phase4_models.py -v`
</acceptance_criteria>

## must_haves
- [ ] Skill demand forecasting model trained
- [ ] Salary prediction model trained
- [ ] v_skill_forecast view created
- [ ] v_salary_prediction view created
- [ ] R² > 0.3 for both models
