---
phase: 4
plan: 05
wave: 5
objective: Implement MLOps basics with model versioning and drift detection
files_modified:
  - src/models/versioning.py
  - src/models/drift.py
  - src/orchestration/retraining.py
  - tests/test_phase4_mlops.py
autonomous: true
---

# Plan 4-05: Orchestration & MLOps Basics

## Objective
Implement model versioning baseline, data drift detection, and monthly retraining flow with Prefect.

## Tasks

### T5.1: Implement model versioning
<read_first>
- src/models/skill_forecast.py (model patterns)
- src/models/salary_predictor.py (model patterns)
</read_first>

<action>
Create `src/models/versioning.py` with:
- `save_model(model, name, version)` function
- `load_model(name, version)` function
- `list_models(name)` function
- Store models in `models/` directory with version tags
- Track model metadata in `models/metadata.json`
</action>

<acceptance_criteria>
- File `src/models/versioning.py` exists
- Functions save_model, load_model, list_models exist
- Models stored with version tags
- Metadata tracked in JSON file
</acceptance_criteria>

### T5.2: Implement drift detection
<read_first>
- src/models/skill_forecast.py (model patterns)
- src/models/versioning.py (versioning patterns)
</read_first>

<action>
Create `src/models/drift.py` with:
- `detect_drift(model_name)` function
- Compare current feature distributions to training data
- Use KS-test for numerical features
- Alert if drift > 10% across multiple features
- Return drift_report dict with drift_detected boolean
</action>

<acceptance_criteria>
- File `src/models/drift.py` exists
- Function `detect_drift()` returns drift_report
- Drift detected when distribution shift > 10%
- Report includes drift_detected boolean and details
</acceptance_criteria>

### T5.3: Create retraining flow
<read_first>
- src/models/skill_forecast.py (training patterns)
- src/models/salary_predictor.py (training patterns)
- src/models/versioning.py (versioning patterns)
</read_first>

<action>
Create `src/orchestration/retraining.py` with:
- `retrain_models()` function
- Load latest models
- Retrain with new data
- Compare metrics to baseline
- Save new version if improved
- Trigger drift detection
</action>

<acceptance_criteria>
- File `src/orchestration/retraining.py` exists
- Function `retrain_models()` retrains both models
- Models versioned after retraining
- Metrics compared to baseline
</acceptance_criteria>

### T5.4: Create MLOps tests
<read_first>
- tests/test_phase2.py (test patterns)
- src/models/versioning.py (versioning patterns)
</read_first>

<action>
Create `tests/test_phase4_mlops.py` with:
- `test_model_versioning()` - saves and loads model
- `test_drift_detection()` - detects drift
- `test_retraining_flow()` - retrains models
- `test_model_metadata()` - tracks metadata
</action>

<acceptance_criteria>
- File `tests/test_phase4_mlops.py` exists
- All tests pass with `python -m pytest tests/test_phase4_mlops.py -v`
</acceptance_criteria>

## must_haves
- [ ] Model versioning working
- [ ] Drift detection implemented
- [ ] Monthly retraining flow created
- [ ] Model metadata tracked
